# Repairs

None. `final.feature.md` is byte-identical to the immutable first draft (`draft.feature.md`, mode 444). Both counterparts were generated by the CLI. All validation and conversion commands exited 0.

The three intended cases, both Examples groups, numeric JSON copies, literal shelf pipe, and single decoded source backslash were verified in generated output.
