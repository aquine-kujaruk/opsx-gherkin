# Draft-to-final repairs

None. `final.feature.md` is an unchanged copy of the immutable first draft.

The requested operation order, exact requirement names, identifiers, and counts are retained. REMOVED carries only its name; RENAMED carries only canonical FROM/TO names. Counterparts are generated by the supplied CLI.
