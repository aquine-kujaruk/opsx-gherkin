Tasks 1–4 record the completed initial implementation. The accepted modeling refinement below supersedes its minimal-example shape and requires fresh verification.

## 1. Capture authoring evidence before changing the guidance

- [x] 1.1 Record the three prompts, supplied domain facts, semantic rubric, original bundle revision, and available agent/model settings described in `design.md` under this change's evaluation evidence; verify the cases cover minimal untagged authoring, complex step arguments/outlines, and all delta operations.
- [x] 1.2 Run the three baseline authoring cases in fresh contexts using the original bundle and CLI; retain first drafts, validator/conversion results, semantic findings, and repairs separately, or record the specific unavailable execution capability without claiming an authoring result.

## 2. Clarify the canonical contract

- [x] 2.1 Read the complete canonical contract, add orientation in the Feature description, and clarify optional descriptive metadata in its existing preservation scenarios; verify the diff retains the established mapping requirements and that the official MDG parser and supported round-trip accept the revised document.
- [x] 2.2 Reduce the contract's outer descriptive tags to the representative placements defined in `design.md`, retaining embedded example tags and every reserved-marker definition; verify the remaining tags survive model comparison through conversion and that no source-metadata stripping behavior is introduced.

## 3. Provide a progressive authoring route

- [x] 3.1 Add the untagged minimal MDG example with a Feature description, one normative Rule, and one Scenario, then generate its OpenSpec and plain Gherkin counterparts with the CLI; verify official endpoint validity, equivalent normalized models, one expanded case, and the intended steps and values.
- [x] 3.2 Revise the skill entry point to orient the agent, retain full-contract reading, distinguish optional metadata from reserved markers, and route basic, complex, and delta needs to their examples; verify every link resolves within a copied bundle and review the guidance against the capability delta's scenarios.
- [x] 3.3 Extend the existing bundled-example checks to cover the minimal family while retaining account and delta coverage; verify model/step assertions and the existing contract, CLI resource, and copied-bundle checks pass without adding tests for exact explanatory wording.

## 4. Evaluate and verify the complete change

- [x] 4.1 Run the same three authoring cases against the revised bundle with matching settings and fresh contexts; deliver a before/after case table with raw drafts, diagnostics, semantic results, and separate repair outcomes, or explicitly document any missing execution evidence.
- [x] 4.2 Review observed authoring errors, correct guidance defects within the agreed scope, and verify every final artifact presented as compatible against both official validators and the intended domain behavior; record any reruns and distinguish the qualitative findings from deterministic conversion guarantees.
- [x] 4.3 Run `openspec validate clarify-agent-authoring-guidance --strict` and `pnpm verify`; verify the packaged profile still matches the canonical resource exactly, bundled references remain self-contained, and no converter semantics, CLI interface, dependencies, or validator versions changed.

## 5. Align teaching specifications with the accepted modeling conventions

- [x] 5.1 Reconcile proposal, design and capability deltas with the user-approved modeling refinement; retain historical evidence and record the MDG formatting change.
- [x] 5.2 Rewrite minimal, account and authentication-delta examples with target metadata, complete scenarios and meaningful argument roles; preserve outcomes and generate counterparts through the CLI.
- [x] 5.3 Reformulate the canonical conversion contract and embedded pair; separate profile access into a companion Feature while retaining mapping and discovery guarantees.
- [x] 5.4 Align self-contained skill guidance and MDG table rendering; extend focused model, argument, metadata, profile, package and separator-input checks without narrowing acceptance.
- [x] 5.5 Complete independent editorial review of all distributed Features and embedded examples, resolve supported findings within two recheck cycles, and record coverage plus final verification evidence.
- [x] 5.6 Run strict OpenSpec validation, skill validation and pnpm verify; confirm packaged resource identity, links and model preservation.
