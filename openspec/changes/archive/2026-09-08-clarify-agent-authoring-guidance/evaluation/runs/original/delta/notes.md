# Notes

Repairs: none. `final.feature.md` is byte-identical to the immutable first draft.

Both counterparts were generated by the supplied CLI without hand edits. ADDED, MODIFIED, REMOVED, and RENAMED operation order and supplied names/values were preserved. Both conversion commands passed endpoint and normalized-model checks.

Direct MDG and plain Gherkin validation passed. Direct OpenSpec validation exited 1 because it requires a main-spec `## Requirements` section; this is a delta with operation sections. The validator usage exposes no delta switch. The generated delta was accepted by both converters. No requirement was changed to satisfy the standalone main-spec check.

Draft SHA-256: `89ed85c4a4bf6ad13c9fdf48e132eec67165984b7937d858a87ac5b407efccc1`
